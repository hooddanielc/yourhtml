import common

cc.flags += [
  '-fprofile-instr-generate',
  '-fcoverage-mapping'
]

link.libs += [
  'gtest',
  'gtest_main',
  'stdc++fs',
  'uuid',
]

link.flags += [
  '-fprofile-instr-generate',
  '-fcoverage-mapping'
]
