import common

cc.flags += [
  '-fprofile-instr-generate',
  '-fcoverage-mapping'
]

link.libs += [
  'gtest',
  'gtest_main'
]

link.flags += [
  '-fprofile-instr-generate',
  '-fcoverage-mapping'
]
